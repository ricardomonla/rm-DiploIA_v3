# 🟢 Hito: 7.4.1 - Carga de Documentos desde Navegador
**Estado**: `[/] En progreso`
**Versión**: `7.4`
**ID Hito**: `7.4.1`
**Fecha**: 2026-01-29

## 📋 Resumen
Implementación de una funcionalidad que permita subir archivos (PDF, Markdown, imágenes) directamente a las carpetas de las clases desde la interfaz administrativa del navegador, automatizando la sincronización del `index.json`.

## 🔄 Tabla de Iteraciones
| ID | Timestamp | Prompt del Usuario | Acción del Agente | Resultado / Estado |
| :--- | :--- | :--- | :--- | :--- |
| IT-01 | 18:15 | `OK inicia. El nuevo plan es implementar la funcionalidad... subida de documentos...` | Inicialización de hito 7.4.1 y análisis de arquitectura. | `[x] Plan propuesto` |
| IT-02 | 18:19 | `OK procede` | Activación de modo EXECUTION y ejecución de tareas técnicas. | `[/] En progreso` |

## 📝 Plan de Implementación (Volcado Procedural)
> [!IMPORTANT]
> Este plan fue aprobado y se vuelca aquí para cumplir con la sincronía de artefactos (`PROC-EJEC-TECN`).

### Propuesta Técnica
- **Backend**: Endpoint `/api/upload` en `server.py` para procesar `multipart/form-data`. Guardado directo en carpetas de clase y auto-sync.
- **Frontend**: Botón de subida en el Modal Admin. Uso de `FormData` para envío asíncrono.
- **Seguridad**: Validación de extensiones permitidas y restricción a entorno local.

### Tareas Técnicas
1. **Server**: Implementar `do_POST` para `/api/upload` en `server.py`.
2. **Script**: Crear `renderUploadUI` y `handleUpload` en `script.js`.
3. **Styles**: Estilizar el control de subida en `styles.css`.

## 🧠 Decisiones Técnicas
- **Backend**: Uso de `cgi.FieldStorage` o procesamiento manual de `multipart/form-data` en `server.py`.
- **Frontend**: Integración de `<input type="file">` en el modal de edición de clases.
- **Seguridad**: Validación de extensiones permitidas (`allowed_extensions` en `index.json`).

---
> [!NOTE]
> Este archivo es parte de la memoria aditiva. No se debe borrar el historial de iteraciones anteriores.
